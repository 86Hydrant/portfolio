/* styles */
import style from "./index.scss";

const HeadlineModule = ({ props }) => {
  return;
  <div className={style.root}>
    <h1>{props.title}</h1>
  </div>;
};

export default HeadlineModule;
